{% block extends %}
{{ "{% extends 'layout.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block body_id '"~ entity_singularized ~"_edit' %}" }}
{{ "{% block main %}" }}
    <h1>{{ '{{ title.edit_'~entity_singularized ~ "|trans({'%id%': " ~ entity_singularized ~ "."~ identifier ~"}) }}" }}</h1>

    {{ "{{ include('_form.html.twig', {
        form: edit_form,
        button_label: 'action.save'|trans,
        include_back_to_home_link: true,
    }, with_context = false) }}" }}

{{ "{% endblock %}" }}

{{ "{% block sidebar %}
    <div class=\"section actions\">
        {{ include('_form.html.twig', {
            form: delete_form,
            button_label: 'action.delete'|trans,
            button_css: 'btn btn-lg btn-block btn-danger',
            show_confirmation: true,
        }, with_context = false) }}
    </div>

    {{ parent() }}
{% endblock %}" }}
{% endblock body %}
